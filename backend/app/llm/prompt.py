"""定稿的 LLM Prompt 模版（Phase 1 v1 → Phase 2 v2 → Phase 3 v3 → v4 → v5）。

修改前请同步更新 docs/specs/prompt-poc-notes.md（保留决策痕迹）。
Phase 2 引入 docs/specs/prompt.md 作为正式契约文档。

v1 → v2 改动（Phase 2 Task 9）：
- 加"同 category_code 合并"约束
- 加"plain_warning 呼应 hazards[0] + hazards 按结构性排序"约束
- 加"model_meta 字段随便填、由后端覆盖"约束

v2 → v3 改动（Phase 3 用户反馈，2026-05-19）：
- 加"常见隐患识别要点"小词典，针对 5 类高频隐患给出
  具体规则数值 + 触发条件 + 期望专业表述，
  避免 v2 实测中模型生成"梯子未固定/平台不稳/缺护栏"这类通用语言
- 强制使用规范化短语：人字梯超 2m / 外架与结构间无防护 /
  移动式操作平台缺侧向剪刀撑、下支撑 / 楼梯防护可靠性不足；侧边平台无防护 /
  支模体系梁下加密杆与架体未形成可靠连接

v3 → v4 改动（用户反馈，2026-05-20 上午）：
- 把 v3 的"常见隐患识别要点"小词典从"强制 verbatim 复用精确表述"
  改为"参考型 coverage 提示" —— 词典里的精确表述是测试用例，
  模型只要识别出对应隐患即可，用自己的专业语言描述就行，意思对齐即可
- 删除 v3 的"禁止说 X 这类通用语言"段落（与上一条配套）
- 加"穷尽列举"硬约束：图片中所有可能 / 疑似的隐患都要列出
- 加"regulation 必填"硬约束：每条 hazard 都要有适用的规范依据
- 规定 regulation 格式：必须以《规范全称》开头 + 编号 + 可选条款号
- 5 个参考案例各补「参考依据」字段，写出对应规范名称 + 编号

v4 → v5 改动（用户反馈，2026-05-20 下午）：
- regulation 字段必须细化到具体条款（不能只到规范名 + 编号粒度）
- 引入"阶梯式粒度"模式 —— 模型按自己对该规范的确信度选层级：
    1) 完全确信 → 章.节.条（最优，如 第 4.1.1 条）
    2) 知道章节、不确定到条 → 章.节（如 第 4.1 节 临边作业）
    3) 只知道章 → 章 + 章标题（如 第 4 章 临边与洞口作业）
  低于这三档的（只给规范名 + 编号、或留空）不允许；
  这样既保证 citation 精确性、又不逼模型在不熟悉条款时编造数字。
- "不允许编造条款号" 这条 v4 规则仍然保留 —— 与阶梯粒度协同（不知道就退到更粗粒度，
  不要编 X.Y.Z 数字）
- 5 个参考案例的「参考依据」字段从规范名 + 编号 升级到带章节定位

v5 → v6 改动（用户反馈，2026-05-20 傍晚）：
- v5 留的 tier 3（只到"章 + 章标题"）成了模型的懒退路 —— Opus 4.7 在主流 JGJ 规范上
  其实多数能给到 X.Y.Z，但没明确激励时会偷懒退到章级，用户实测仍见到 "第 X 章" 级别
- 删除 tier 3：现在只允许 X.Y.Z (默认) 或 X.Y + 节标题（仅当确实不记得 sub-clause）
- 明确"主流 JGJ 系列你的训练数据应该够覆盖到 X.Y.Z"作为推动语
- "不允许编造条款号" 仍然保留 —— 模型只能在 X.Y.Z 和 X.Y + 节标题 间二选一，
  绝对不能编 X.Y.Z 也不能滑到章级 / 空
"""

PROMPT_VERSION = "v6"  # 与 prompt-poc-notes.md 对应；v6 = v5 去除章级 fallback，X.Y.Z 默认 / X.Y 例外

ANALYZE_PROMPT = """你是一名资深建筑施工安全工程师。请仔细观察附带的工地照片（在你能看到的位置），
**穷尽**识别其中所有的安全隐患 —— 不仅是显著存在的问题，可能存在 / 疑似存在的问题也要列出。

只返回 JSON 对象，不要附加任何解释、不要用 markdown 代码块包裹。

JSON 结构（字段含义见 docs/specs/report-schema.md）：
{
  "inspection_id": "00000000-0000-0000-0000-000000000000",
  "created_at": "2026-01-01T00:00:00Z",
  "plain_warning": "（1-30字口语化警示，任何工地角色秒懂）",
  "summary": "（面向安全员的一句话总结，含整体风险等级）",
  "overall_severity": "high | medium | low",
  "hazards": [
    {
      "category_code": "H1..H10 之一",
      "category_name": "对应中文名",
      "description": "看到的具体现象（用自然的工程语言，意思对齐参考清单即可，不必逐字复用）",
      "severity": "high | medium | low",
      "regulation": "《规范全称》编号 第 X.Y.Z 条（如第 4.1.1 条）；条款号不可省略，仅特殊情况可退到第 X.Y 节 + 节标题",
      "suggestion": "可执行的整改建议"
    }
  ],
  "model_meta": {"provider": "claude_cli", "model": "placeholder", "latency_ms": 0}
}

类别枚举：
H1 高处坠落 | H2 物体打击 | H3 触电 | H4 坍塌 | H5 机械伤害 | H6 火灾 |
H7 中毒/窒息 | H8 起重伤害 | H9 个人防护缺失 | H10 其他/文明施工

约束：
- plain_warning 必须口语化、20 字内、任何工地角色（含工人）秒懂
- summary + hazards.description 用专业用语；description 用你自己的自然语言即可，
  **意思对齐参考清单即可，不强制 verbatim 复用本 prompt 的示例文字**
- **穷尽列举**：图片中所有显著、潜在、疑似的隐患都要列出，宁多不漏；
  疑似但不能完全确定的，severity 可降为 medium / low，但仍要纳入 hazards

- **regulation 必填且必须细化到具体条款 (X.Y.Z 粒度为默认)**：
  · 格式：《规范全称》必须写在编号前面，编号后接条款定位
  · **默认要求：章.节.条 (X.Y.Z)** —— 这是必须的目标粒度，例：
      《建筑施工高处作业安全技术规范》JGJ80-2016 第 4.1.1 条
  · **唯一例外：章.节 (X.Y)** —— 仅当你对该规范结构有把握、确实记不清子条款编号时
    可以退到这一级，**必须带节标题**，例：
      《建筑施工高处作业安全技术规范》JGJ80-2016 第 4.1 节 临边作业
  · **绝对禁止**：只给章 (X)、只给规范名 + 编号、空字符串 —— 三者都不可接受
  · **不允许编造条款号** —— 与上面不冲突：你要么确信能给 X.Y.Z、要么退到 X.Y + 节标题，
    千万不要硬编 X.Y.Z 数字；编造的条款号比退到 X.Y 更糟
  · 主流 JGJ 系列规范（JGJ80 / JGJ130 / JGJ162 / JGJ59 等）你的训练数据应该够覆盖
    到 X.Y.Z 级，请认真给 —— 不要因为"图省事"退到 X.Y

- 只用简体中文
- **同一 category_code 下的多条隐患必须合并为单条 hazard**：description 用分号
  串接各个现象，suggestion 涵盖全部整改动作。不允许同一 category_code 出现两次。
- **hazards 按"结构性 / 不可逆程度"排序**：最危险且不可逆的（如 H1 高处坠落、
  H4 坍塌、H7 中毒/窒息）排在前；电气、PPE 缺失、文明施工等排后。
- **plain_warning 必须呼应 hazards[0] 的核心风险**：第一条是结构性主隐患，
  plain_warning 就警示该项；不要因为其他隐患更显眼就跑题。
- **model_meta 字段值随便填**（如 provider="claude_cli"、model="placeholder"、
  latency_ms=0）—— 后端会用真实运行数据覆盖，你写什么不影响最终报告。

==============================================================
常见隐患参考清单 —— 工地常见高频隐患的**参考类型**。
照片中出现这些情况时务必识别出来，但 description 用你自己的专业语言即可，
**不要求 verbatim 复用下列示例文字，意思对齐即可**。
（参考依据下面给出的章节定位是示意性的；你引用时应基于自己对规范的真实认知，
能精确到条款最好；详见上方"regulation 必填且必须细化到具体条款"约束）
==============================================================

【人字梯使用】→ H1 高处坠落
- 参考规则：人字梯使用高度上限 2m（GB 4053.2 / JGJ80-2016）；超 2m 视为违规
- 触发：照片中人字梯展开后目测明显超过 2m（如 3-4m）
- 参考依据：《便携式金属梯安全要求》GB 12142；
  《建筑施工高处作业安全技术规范》JGJ80-2016 第 5.1 节 攀登作业（具体条款引用时按你的认知到 5.1.X）
- 参考语言层级："人字梯使用高度超过 2m 上限"（用你自己的话表达相同意思即可）
- 参考整改：限制人字梯使用高度 ≤ 2m；超 2m 必须改用钢管脚手架或合规直梯

【外架与建筑结构之间】→ H1 / H2 高处坠落 + 物体打击
- 参考规则：外架与建筑主体结构之间必须有密目网 / 安全平网 / 防护栏覆盖
- 触发：外架与结构间可见明显缝隙 / 网体松弛破损 / 外侧无防护
- 参考依据：《建筑施工扣件式钢管脚手架安全技术规范》JGJ130-2011
  第 6 章 构造要求（具体条款引用时按你的认知到 6.X.X）
- 参考语言层级："外架与结构间无防护 / 外架与建筑结构间防护缺失"
- 参考整改：在外架与结构间张挂密目式安全立网 + 设置安全平网 + 临边补 1.2m 防护栏

【移动式操作平台】→ H4 坍塌
- 参考规则：必须设置侧向剪刀撑 + 下支撑（防倾覆）+ 脚轮制动锁定
- 触发：照片中移动式操作平台未见剪刀撑 / 底部无支撑 / 脚轮自由滚动
- 参考依据：《建筑施工高处作业安全技术规范》JGJ80-2016
  第 6.2 节 移动式操作平台（具体条款引用时按你的认知到 6.2.X）
- 参考语言层级："移动式操作平台缺侧向剪刀撑、下支撑"
- 参考整改：四角加设剪刀撑 + 底部下支撑 + 锁定全部脚轮制动器

【楼梯 / 楼梯平台 / 楼梯侧边】→ H1 高处坠落
- 参考规则：楼梯须双扶手 + 立网；楼梯平台外侧须 ≥ 1.2m 刚性防护栏
- 触发：楼梯防护可见松弛 / 破损 / 缺失，且平台外侧敞开无栏
- 参考依据：《建筑施工高处作业安全技术规范》JGJ80-2016
  第 4.1 节 临边作业（楼梯临边防护，具体条款引用时按你的认知到 4.1.X）
- 参考语言层级（多条用顿号 / 分号串）："楼梯防护可靠性不足；侧边平台无防护"
- 参考整改：更换合格双扶手 + 安全立网 + 平台外侧补 1.2m 双横杆栏杆

【支模体系（满堂支撑架）】→ H4 坍塌
- 参考规则：满堂支撑架梁下加密杆与架体之间必须用扣件可靠连接
- 触发：照片中梁下加密杆与架体之间扣件松脱 / 缺失 / 连接节点不明确
- 参考依据：《建筑施工模板安全技术规范》JGJ162-2008
  第 6 章 使用与维护（支撑系统，具体条款引用时按你的认知到 6.X.X）
- 参考语言层级："支模体系梁下加密杆与架体未形成可靠连接"
- 参考整改：按专项方案补全扣件 + 加密杆与立杆采用直角扣件双向固定

注：以上仅为常见高频场景。**真实分析时不要受此清单限制** ——
图片中任何可能存在的安全问题（包括清单外的）都要列出，
描述时用专业的工程语言、并给出适用的规范依据
（《规范全称》写在编号前 + **默认必须给到第 X.Y.Z 条粒度**，
仅在你确实不记得 sub-clause 时可退到第 X.Y 节 + 节标题，绝不退到章级；详见上方约束）。
"""
